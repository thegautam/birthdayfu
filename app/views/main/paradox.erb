<!--ul>
  <% @dated_friends.each do |friend| %>
     <%= friend['name'] %>
     <%= friend['birthday'] %>
     <%= friend['bday'] %>
     <br/>
  <% end %>

  <% @bday_hashes.each do |key, value| %>
     <%= "#{key} => #{value}" %>
     <br/>
  <% end %>

  <p>Matches <%=@matches%></p>
  <p>  Total <%=@total%></p>
</ul-->

<table>
  <% @months.each do |month| %>
     <tr><p><%= month %></p></tr>
  <% end %>
<table>
